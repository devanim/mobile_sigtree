import { StyleSheet } from "react-native";

export const termsOfServiceStyles = StyleSheet.create({
  containerCard: {
    backgroundColor: "#fff",
    marginTop: 10,
    marginleft: 5,
    borderRadius: 15,
    height: 500,
    width: 500
  }
});