import { StyleSheet } from "react-native";

const carouselCardStyles = StyleSheet.create({
  container: {
    backgroundColor: "#FFF",
    height: 50,
    width: 200,
    margin: 2
  },
  text: {
    fontSize: 15,
    fontWeight: "bold",
    justifyContent: "center",
    textAlign: "center",
  }
});

export default carouselCardStyles;