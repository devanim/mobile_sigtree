import { StyleSheet } from "react-native";

const errorStyles = StyleSheet.create({
  container: {
    backgroundColor: "#FFF",
    width: "100%",
    height: "100%"
  }
});

export default errorStyles;