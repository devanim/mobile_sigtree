import { StyleSheet } from "react-native";

const custCheckbokStyles = StyleSheet.create({
  section: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  paragraph: {
    fontSize: 15,
    color: "#ff0000"
  },
  checkbox: {
    margin: 8,
  },
});

export default custCheckbokStyles;