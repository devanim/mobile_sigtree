import { StyleSheet } from "react-native";

export const dashboardStatisticsStyles = StyleSheet.create({
  content: {
    paddingBottom: 10,
  },
  text: {
    fontSize: 15,
    fontWeight: "bold",
    color: "#FFF"
  }
});