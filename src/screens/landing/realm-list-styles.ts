import { StyleSheet } from "react-native";

export const realmListStyles = StyleSheet.create({
  contentContainerStyle: {
    paddingHorizontal: 32,
    paddingBottom: 60,
  },
  button: {
    marginTop: 8,
  },
  horizontalView: {
    flex: 2,
    flexDirection: "row" ,
    marginLeft:20
  }
});