import { StyleSheet } from "react-native";

export const articleBriefStyles = StyleSheet.create({
  containerCard: {
    backgroundColor: "#fff",
    marginTop: 10,
    borderRadius: 15
  },
  default: {
    margin: 8,
    color: "#000",
  }
});