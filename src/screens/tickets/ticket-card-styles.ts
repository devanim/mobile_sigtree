import { StyleSheet } from "react-native";

export const ticketCardStyles = StyleSheet.create({
  containerCard: {
    backgroundColor: "#fff",
    marginTop: 10,
    borderRadius: 15
  },
  button: {
    marginTop: 8,
  },
  content: {
    height: 350,
    width: 350,
    overflow:'hidden'
  }
});