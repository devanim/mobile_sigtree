import { StyleSheet } from "react-native";

const tosBuildingStyles = StyleSheet.create({
  container: {
    backgroundColor: "#000",
    marginBottom: 2,
  },
  text: {
    color: "#FFF",
    marginBottom: 2,
    fontSize: 15,
    fontWeight: "bold"
  },
  halfWidth: {
    width: "50%"
  }
});

export default tosBuildingStyles;