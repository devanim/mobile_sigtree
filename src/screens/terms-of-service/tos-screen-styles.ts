import { StyleSheet } from "react-native";

export const tosScreenStyles = StyleSheet.create({
  container: {
    paddingBottom: 2,
    backgroundColor: "#000"
  },
  text: {
    backgroundColor: "#FFF",
    fontSize: 15,
    fontWeight: "bold",
    marginBottom: 2
  }
});